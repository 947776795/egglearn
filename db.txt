user
用户表：用于存储用户的账号信息，包括用户名、密码、电子邮箱等。可以使用用户ID作为主键。

文章表：用于存储博客文章的基本信息，包括文章标题、摘要、正文、发布时间、作者等。可以使用文章ID作为主键。

标签表：用于存储文章的标签信息，包括标签名称、标签描述等。可以使用标签ID作为主键。

评论表：用于存储用户对文章的评论信息，包括评论内容、评论者、评论时间等。可以使用评论ID作为主键。

点赞表：用于存储用户对文章的点赞信息，包括用户ID、文章ID、点赞时间等。